import { Meta, Story } from '@storybook/blocks';
import * as RxIconStories from './icon.stories';

<Meta title='Misc/RxIcon' />

# Documentation for RxIcon

## Overview
The `RxIcon` component is designed to display icons in Angular applications. It supports various features such as customizable sizes and colors.
By default, It uses the library Rx-icons (Remanx-icons) but you can add yours if you want to. He can read a json format or css files.
You can use both implementations and with multiple sources.

Warning : The other components use the Rx-icons library.

## Features
- **Customizable Size**: Easily define the size of the icon.
- **Color Options**: Set the color of the icon to match your application's theme.
- **Responsive Design**: The icon adapts to different screen sizes.

## Usage
To use the `RxIcon`, import it into your Angular module:

```typescript
import { RxIcon } from '@lynqis/remanxng/components';
```

```html
<rx-icon [iconJson]="'home'" />
<rx-icon [iconJson]="'eye'" [stroke]="'red'" />
<rx-icon [iconJson]="'info'" [thickness]="'3'" />
<rx-icon [iconJson]="'menu'" [stroke]="'green'" />
<rx-icon [iconJson]="'save'" [fill]="'red'" />
<rx-icon [iconJson]="'rx-leanqis'" [fill]="'red'" [stroke]="'red'" [$height]="'3rem'" [$width]="'3rem'" />
```

<Story name="Basic" of={RxIconStories.SimpleExemple} />

## Props
| Property         | Type             | Description                               |
|------------------|------------------|-------------------------------------------|
| `stroke`           | `string`         | Defines the color of the icon's stroke.            |
| `thickness`          | `string`         | Defines the thickness of the icon's stroke.           |
| `fill`          | `string`         | Defines the color of the icon's fill.           |
| `$height`          | `string`         | Defines the icon's height.           |
| `$width`          | `string`         | Defines the icon's width.           |
| `svgClasses`          | `string`         | Defines the icon's css classes.           |
| `iconJson`          | `string`         | Call json implentation.           |
| `iconCss`          | `string`         | Call css implentation.           |


## RxIcons (Remanx-icons)

By default, remanxng use Remanx-icons, a svg icons library.

<Story name="Basic" of={RxIconStories.AllIcons} />
